#-----------------List Comprehension -------------------------

# symbols = '$¢£¥€¤'
# list_data = list(filter( lambda x : x > 127, map(ord, symbols)))

# name = ['vicky', 'amar']
# rank = [1,2,3]
# [(n , r) for n in name for r in rank ]

# symbols = '$¢£¥€¤'
# tuple_data = tuple(ord(symbol) for symbol in symbols)

